output: 
```markdown
# HU7877 - Registrar foto al usuario

## Descripción
Como usuario, quiero poder registrar una foto asociada a mi perfil, para personalizar mi experiencia en la aplicación y facilitar el reconocimiento.

## Criterios de Aceptación
1. Se debe crear un endpoint POST en la ruta `/users/{userId}/photo` que permita subir una foto para el usuario especificado.
2. La solicitud debe incluir la foto en formato multipart/form-data, junto con cualquier otro dato necesario (por ejemplo, un token de autenticación).
3. El backend debe validar que el archivo subido sea una imagen (por ejemplo, formatos JPEG, PNG) y que no exceda un tamaño máximo definido (por ejemplo, 5 MB).
4. Si la foto se registra correctamente, el endpoint debe devolver un código de estado HTTP 200 y un objeto que confirme la actualización de la foto del usuario.
5. Si hay un error en la validación (por ejemplo, tipo de archivo incorrecto o tamaño excesivo), el endpoint debe devolver un código de estado HTTP 400 y un mensaje de error claro.
6. Se debe manejar adecuadamente cualquier error que pueda ocurrir durante la ejecución del endpoint, devolviendo un mensaje de error claro y un código de estado HTTP apropiado.

## Tareas
- [ ] [BE] Crear endpoint POST /users/{userId}/photo para registrar la foto del usuario
```
