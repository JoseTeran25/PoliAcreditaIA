output: 
```markdown
# HU7767 - Buscar Criterios EUR-ACE

## Descripción
Como usuario, quiero poder buscar Criterios EUR-ACE utilizando filtros por código y descripción, para facilitar la localización de información específica en la lista de criterios.

## Criterios de Aceptación
1. Se debe adaptar el endpoint existente para listar Criterios EUR-ACE para que acepte parámetros de consulta opcionales `codigo` y `descripcion`.
2. Si se proporciona el parámetro `codigo`, el endpoint debe filtrar los resultados para incluir solo aquellos criterios cuyo código coincida con el valor proporcionado.
3. Si se proporciona el parámetro `descripcion`, el endpoint debe filtrar los resultados para incluir solo aquellos criterios cuya descripción contenga el valor proporcionado.
4. La respuesta del endpoint debe ser un JSON que contenga una lista de objetos, donde cada objeto representa un Criterio EUR-ACE, incluyendo al menos los siguientes campos:
   - id
   - codigo
   - nombre
   - descripcion
5. Si no se encuentran criterios que coincidan con los filtros aplicados, el endpoint debe devolver un array vacío.
6. Se debe manejar adecuadamente cualquier error que pueda ocurrir durante la ejecución del endpoint, devolviendo un mensaje de error claro y un código de estado HTTP apropiado.

## Tareas
- [7795] [BE] Añadir capacidad de filtrado por código y descripción al endpoint de listar criterios
```
