output: 
```markdown
## HU7777 - Visualizar Matriz de Mapeo OPP vs RA

### Descripción
Como usuario, quiero visualizar la matriz de mapeo entre Objetivos de Programa (OPP) y Resultados de Aprendizaje (RA), para entender mejor las relaciones y justificaciones entre ellos.

### Criterios de Aceptación
- Se debe crear un endpoint GET `/mappings/opp-ra` que devuelva los datos de la matriz de relaciones OPP-RA, incluyendo información relevante sobre cada relación.
- Se debe exponer un catálogo de OPP por programa a través del endpoint GET `/programs/{programId}/opps?lang=es`, que incluya los campos id, code, name, description y active.
- Se debe exponer un catálogo de RA por programa a través del endpoint GET `/programs/{programId}/learning-outcomes?lang=es`, que incluya los campos id, code, name, description y active.
- La respuesta de los endpoints debe ser clara y estructurada, permitiendo al usuario acceder fácilmente a la información necesaria para visualizar la matriz.

### Tasks
- [7856] [BE] Crear endpoint GET /mappings/opp-ra para obtener datos de matriz de relaciones OPP-RA
- [7872] [BE] Exponer catálogo de OPP por programa — GET /programs/{programId}/opps?lang=es con id, code, name, description, active.
- [7873] [BE] Exponer catálogo de RA por programa — GET /programs/{programId}/learning-outcomes?lang=es con id, code, name, description, active.
```
