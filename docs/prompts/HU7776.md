output: 
```markdown
# HU7776 - Paginar Criterios EUR-ACE

## Descripción
Como usuario, quiero poder paginar los Criterios EUR-ACE al listar, para facilitar la navegación y visualización de grandes conjuntos de datos.

## Criterios de Aceptación
1. Se debe implementar la lógica de paginación en el endpoint existente para listar Criterios EUR-ACE.
2. El endpoint debe aceptar parámetros de consulta opcionales `page` y `limit`:
   - `page`: el número de la página que se desea obtener (por defecto 1).
   - `limit`: el número máximo de resultados por página (por defecto 10).
3. La respuesta del endpoint debe incluir:
   - Un array de objetos que representan los Criterios EUR-ACE en la página solicitada.
   - Un objeto que contenga información sobre la paginación, incluyendo:
     - `totalItems`: el número total de Criterios EUR-ACE disponibles.
     - `totalPages`: el número total de páginas disponibles.
     - `currentPage`: la página actual solicitada.
     - `itemsPerPage`: el número de elementos por página.
4. Si se solicita una página que no existe (por ejemplo, un número de página mayor al total de páginas), el endpoint debe devolver un código de estado HTTP 404 y un mensaje claro indicando que no se encontraron resultados.
5. Se debe manejar adecuadamente cualquier error que pueda ocurrir durante la ejecución del endpoint, devolviendo un mensaje de error claro y un código de estado HTTP apropiado.

## Tareas
- [7802] [BE] Implementar lógica de paginación en el endpoint de listar criterios
```
