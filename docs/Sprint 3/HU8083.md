output: 
```markdown
# HU8083 — Listar Asignaturas (PEA)

## Descripción
Como usuario, quiero poder listar todas las asignaturas disponibles en el sistema, de manera que pueda ver la oferta académica y seleccionar las que me interesen.

## Criterios de Aceptación
1. El sistema debe permitir la consulta de un listado de asignaturas, mostrando información relevante como nombre, código y estado.
2. La consulta debe tener en cuenta los permisos del usuario, mostrando solo aquellas asignaturas a las que el usuario tiene acceso.
3. El sistema debe manejar correctamente los casos en los que no existan asignaturas disponibles para el usuario.

## Tareas (Backend)
- [ ] Crear un endpoint API `GET /api/subjects` para obtener el listado de asignaturas (Tarea ID: 8090).
- [ ] Implementar la lógica de consulta de asignaturas por permisos, asegurando que solo se devuelvan aquellas a las que el usuario tiene acceso (Tarea ID: 8091).
- [ ] Implementar pruebas unitarias para asegurar que la consulta de asignaturas funciona correctamente y que se respetan los permisos del usuario.
- [ ] Documentar el nuevo endpoint y su uso en la API.

## Tareas (Frontend)
- [ ] Crear una interfaz que muestre el listado de asignaturas obtenidas del backend.
- [ ] Implementar la lógica para enviar la solicitud al backend y manejar la respuesta con el listado de asignaturas.
- [ ] Mostrar un mensaje adecuado en caso de que no se encuentren asignaturas disponibles para el usuario.
- [ ] Permitir la selección de asignaturas desde el listado para su posterior uso en otras funcionalidades del sistema.
```
